<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- <link rel="stylesheet" href="style.css"> -->
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm" href="/logout">Logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="w-10 h-10  rounded-md">
                <img class="w-full h-full object-cover rounded-xl" src="/images/uploads/<%=user.profilepic %>" alt="">
            </div>
        <h3 class="text-3xl  mb-5"> <span class="font-light"> Hi,</span> <%= user.name %> </h3> 
        </div>
        

        <h5>You can create a new post.</h5>
        <form method="post" action="/post">
            <textarea placeholder="Whats on your mind ?" class=" mt-5 p-3 block outline-none w-1/3 border-zinc-800 resize-none bg-transparent border-2 rounded-md" name="content" id=""></textarea>
            <input class="px-3 py-2 bg-blue-500 rounded-md block mt-2 w-44 " type="submit" placeholder="Create New Post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainers mt-5">  
                <% user.posts.forEach(function(post){ %>
                <div class="post w-1/3 p-4 rounded-md bg-zinc-800 border-[1px] border-zinc-700 mb-4">
                  
                    <h4 class="text-blue-500 mb-2">@<%=user.username %></h4>
                    <p class="text-sm tracking-tight"> <%= post.content %> </p>
                <div class="btns flex gap-4 mt-5">
                    <a class="text-blue-500" href="">Like</a>
                    <a class="text-zinc-600" href="">Edit</a>
                </div>
               
                </div>
                <% }) %>
            </div>
        </div>
       
    </div>
    
</body>
</html>